def testing_testing():
    message = (f"Do you want to convert all dates in filenames to ISO format (YYYY-MM-DD)?"
    f" (y/n): ")
    input(message)

    print(f"Row1"
          f"Row2")
if __name__ == "__main__":
    testing_testing()